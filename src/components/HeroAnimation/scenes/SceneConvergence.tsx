import { SceneProps } from '../types';

export function SceneConvergence(_props: SceneProps) {
  return (
    <div className="absolute inset-0 z-30 flex items-center justify-center">
      <h2 className="text-5xl sm:text-7xl md:text-[8rem] font-black text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-purple-600 tracking-tighter">
        CONVERGENCE
      </h2>
    </div>
  );
}
